{
  "compilerOptions": {
    "strict": true,
    "target": "ES2020",
    // CommonJS so Electron's sandboxed preload can execute it (no import statements)
    "module": "CommonJS",
    "outDir": "../../dist-electron",
    "skipLibCheck": true
  },
  "include": ["preload.ts"]
}
